# 极光观测机会助手：分区开发指南

更新：2026-09-24。本文是内部中文开发指南，与 PROJECT_PLAN.md 配套使用。

## 怎么使用这份指南

按功能分区开发，每一区一起完成所需的后端、前端和验证。每次只推进一个明确的小任务，通过该区验收后再进入下一块。本文是执行计划，复选框未勾选的内容均不代表已经实现。

当前基线（2026-09-25）：01 区实现已完成，远程 CI 尚未接入；02 区地点和当地夜晚基础规则已完成；03 区 NOAA OVATION 与 Kp 已接入，但观测数据最大允许延迟仍待更多样本；04 区 MET Norway 云量数据、缓存和页面展示已接入并通过三地样本验证；05 区太阳高度计算和候选黑暗时段已完成，并通过参考点、高纬和时间边界测试；06.1 统一事实接口和三晚页面已完成，06.2 地图交互与端到端验收仍有未完成项；整晚肉眼观测等级仍保持“数据不足”。08 区尚未设计运行记录存储，09 区 Agent 尚未接入。

## 分区总览

| 分区 | 目标 | 主要依赖 | 完成后的可见成果 | 当前状态 |
| --- | --- | --- | --- | --- |
| 01 基础修正与工程管理 | 修正已知错误，建立清楚的职责和验证方式 | 当前骨架 | 搜索选择流程稳定，代码可追踪 | 实现完成；远程 CI 待接入 |
| 02 地点与当地夜晚 | 确认地点、时区、今晚和三晚的时间边界 | 01 | 同名地点不误选，跨时区日期正确 | 已完成基础定义与边界验证 |
| 03 NOAA 极光与地磁数据 | 获取并规范化 OVATION、Kp 事实 | 01；来源验证 | 能查看两类数据各自的时间和状态 | 两类接口、缓存及来源时间已接入；时效判定/异常样例待完成 |
| 04 天气数据 | 合规获取云量并处理缓存与失败 | 01、02；已提供联系身份 | 展示有来源、有时间的云量序列 | Provider、缓存和页面接入已完成；持续 SLA 未验证 |
| 05 太阳高度与黑暗时段 | 验证天文计算和高纬度边界 | 02；计算方法验证 | 太阳高度与候选黑暗时段可核对 | 已完成：后端计算、三种候选阈值、中英页面及边界验证 |
| 06 事实汇总、三晚页面与地图 | 把同一时间范围内的事实呈现给用户 | 02–05；地图来源核对 | 可查询三晚事实和有效短时地图 | NOAA 地图已接入；本地浏览器验收通过，地图交互与时效处理待完成 |
| 07 观测等级规则 | 实现有依据、可解释、版本化的判定 | 06；阈值和适用性验证 | 等级带原因、时效和规则版本 | 未实现，受验证门槛约束 |
| 08 运行记录与持久化 | 保存可追溯的判定与来源依据 | 07 的数据契约；存储决策 | 能追查一次判定，加入 Repository、Entity | 待设计 |
| 09 Agent 问答 | 通过只读工具解释同一套事实和等级 | 06–08；模型与成本决策 | 页面与 Agent 结果一致，有工具轨迹 | 待开始 |
| 10 部署、端到端验收与演示 | 验证线上完整流程并形成证据 | 01–09 | 可演示的 MVP 和真实验证记录 | 待开始 |

推荐顺序：01 → 02 → 03 → 04 → 05 → 06 → 07 → 08 → 09 → 10。

03、04、05 在各自依赖满足后可独立推进，不要求同时开发。MET Norway 的联系身份已提供，04 区的请求配置和取样可以在 03 区完成后推进；05 区也可独立验证。07 的科学依据暂不足时，可以完善 06 的事实展示和 08 的数据模型草案，但不能宣称整晚观测等级已完成。

计划部署平台一旦确定，应提前做提供商网络探测并保存结果。提供商在部署环境的可用性不能拖到最终上线才检查；10 区再验证完整线上流程。开发指南不指定尚未决定的平台，也不授权购买服务。

## 01 区：基础修正与工程管理

**目标：**把已有搜索流程修正到可以放心继续扩展。

**任务顺序：**

- [x] 修复选择成功后仍出现“没有匹配地点”的提示；分别表示初始、加载、空结果、错误和已选择状态。
- [x] 建立 LocationService（地点服务），让地点搜索和获取都经过 Service。Controller 只处理 HTTP 输入输出。
- [x] 查询先去除首尾空白，再检查长度和空白值；地点 ID 必须为正数。
- [x] 区分无结果、无效外部响应、超时、限流和提供商不可用；检查 JSON 字段类型、坐标范围和 IANA 时区。
- [x] 将 Clock（时钟）和可配置的 HTTP 客户端通过依赖注入传入，使边界时间和外部失败可以测试。
- [x] 适度拆分前端 API 调用、类型、地点搜索和三晚卡片；开启 TypeScript 严格检查并处理实际报错；整理难读的 JSX 和 CSS。
- [x] 初始化 Git 并建立基线提交，检查忽略规则，清理不再使用的模板资源。
- [x] 配置并验证本地检查命令。
- [ ] 远程仓库建立并推送后，接入 CI。

**01 区进度记录（2026-09-24）：**本区实现项和本地验收已完成。补充了注入式 HTTP 客户端及连接/请求超时配置、固定时钟三晚回归测试、TypeScript `strict` 检查和本地构建/lint/test 命令；移除了未引用的 React/Vite 模板图片。远程仓库 `alisonzzzz-y/aurora-finder` 已建立，最近功能提交已推送；目前未发现 GitHub Actions 工作流，CI 仍待接入。

**涉及位置：**backend 的 controller、service、provider、config 包；frontend/src 中的 API、类型和组件文件。类名和目录可按实际复杂度调整，不要求先建大量空文件。

**验收：**正常选择后没有空结果提示；空查询得到明确的客户端错误；无效提供商响应不会冒充“无匹配地点”；提供商故障得到一致的不可用响应。为这些具体缺陷补回归测试，前后端构建通过。

**本区交付：**一条可靠的地点搜索请求链、已修复问题记录和可重复的检查命令。

## 02 区：地点与当地夜晚

**目标：**明确“在哪里”和“哪一晚”。

- [x] 验证 Open-Meteo 对北欧、爱尔兰、南半球、日期线附近和非城市查询的覆盖；记录不支持的输入边界。
- [x] 地点候选显示名称、行政区、国家和时区，必要时补坐标；不自动选择同名地点。
- [x] 明确首版是否支持任意坐标或地图点选。如果只支持地名候选，应把界面和文档中的覆盖描述写准确；任意坐标的时区解析不能靠附近城市猜测。
- [x] “今晚”的日期按地点当地当前日历日期；之后两晚按当地日历递进。页面日期和时刻按当地时间显示，并标注对应 `UTC±HH:MM` 偏移。
- [x] 明确“今晚”日期锚点：按地点当前的当地日历日期。当地凌晨查询时，仍对应当天稍后到来的夜间；前一晚不混入当天。日历日期与实际太阳黑暗时段分开。
- [x] 定义当地夜晚的候选时间区间及其 UTC 起止时间；区间不等于已经计算好的黑暗时段。
- [x] 缓存地点搜索和已选择地点，合并相同的并发请求；按提供商约束配置限流、缓存期限和重试策略。
- [x] 页面解释日期所属时区；查询词更新或切换地点后，旧请求和旧地点结果不混入当前界面。

**建议职责：**LocationService 负责地点；NightWindowService（夜晚时间服务）负责时间区间；Provider 负责外部地点响应。

**验收：**用固定时间测试当地午夜前后、夏令时转换和日期线两侧；验证三个夜晚按当地日历递进，不把每天固定加 24 小时当成所有地区都正确。选定地点的 ID、坐标、时区保持一致。

**本区交付：**地点契约、夜晚定义和时间边界测试。

**夜晚日期决策（2026-09-24）：**“今晚”卡片锚定地点当前的当地日历日期，与 PROJECT_PLAN.md 的日期定义一致。若当地时间已过午夜，卡片仍表示当天稍后到来的夜间，不回退到前一个日期。此决定只定义夜晚所属日期，不假定固定的观测小时数，也不代替后续太阳高度/黑暗时段计算。`OutlookService` 按地点时区生成当地今天及之后两天，并为每晚提供候选窗口的 UTC 起止区间。

**候选夜晚区间决策（2026-09-24）：**每个当地日期 D 使用当地时间 `[D 12:00, D+1 12:00)` 作为事实匹配的候选窗口，并在 API 中提供 UTC 起止 `evaluationWindowStartUtc` / `evaluationWindowEndUtc`。以当地中午为边界可把 D 日傍晚到次日清晨归在同一张日期卡片；这是数据查询窗口，不表示 24 小时都黑暗，也不代表用户整段时间都能观测。实际太阳黑暗时段由 05 区单独计算。起止分别按 IANA 时区换算，因此跨夏令时切换的窗口可为 23 或 25 小时。

**02 区进度记录（2026-09-24）：**完成当地凌晨的日期锚点定义，并在三晚响应增加候选窗口的 UTC 起止时间。`OutlookServiceTest` 覆盖 Dublin 普通日期以及 2026 年秋季夏令时回拨时的 25 小时和次日 24 小时窗口。等级继续为 `INSUFFICIENT_DATA`；候选区间不计算黑暗时段或观测概率。

**02 区时间边界验收（2026-09-24）：**固定时钟测试覆盖当地午夜前后、Dublin 春季夏令时跳前的 23 小时窗口、秋季回拨后的 25 小时窗口及次日 24 小时窗口；同一 instant 下 Apia 与 Pago Pago 的本地日期分别正确为 2026-09-24 与 2026-09-23，偏移分别为 UTC+13 与 UTC-11。后端 18 项测试全部通过。

**地点缓存和请求控制进度（2026-09-24）：**搜索结果按去除空格并忽略大小写后的查询缓存 10 分钟，搜索结果中的地点记录及 `/get` 记录缓存 1 小时；每类最多 256 条，参数可从应用配置调整。相同的并发查询与地点读取共用同一个进行中请求。Open-Meteo 当前免费额度为每天 10,000、每小时 5,000、每分钟 600 次；单实例默认上限设为 9,000 / 4,500 / 500，留出余量并按滚动时间窗拒绝超额本地请求。失败结果不缓存，不自动重试，以免放大额度消耗。该限流器为进程内状态，多实例部署时需要共享配额计数或按实例分配总预算。测试覆盖查询并发合并、既有缓存复用和本地限流拒绝。

**旧结果竞态修正（2026-09-24）：**搜索词变化时立即取消前一搜索并清空旧候选；搜索请求和地点三晚请求均携带 `AbortSignal`，提交结果前检查当前请求身份。正在读取三晚数据时锁定搜索输入；切回搜索页后旧地点卡片与此前查询词不会保留。Chrome 本机页面已检查 Dublin 搜索候选、切换为 Galway 后显示 `Europe/Dublin` 与当地时间偏移，再切回搜索页确认地点结果已清除。前端构建与 lint 通过。

**02 区进度记录（2026-09-24）：**完成地点搜索覆盖抽样，结果和搜索边界见 `docs/phase-0-data-and-rules.md`。样本覆盖 Tromsø、Galway、Ushuaia、Apia、Longyearbyen，以及 Mount Everest 和 Svalbard Archipelago 等非城市地名；还核对了无匹配查询。发现同名和前缀结果可能把异国地名排在目标之前。候选行现显示地点名、二级/一级行政区、国家、经纬度和 IANA 时区，仍由用户显式选择。首版只支持地点搜索和全球地图查看，不支持地图点选或任意经纬度。地点搜索不提供任意地图坐标的时区解析，部署端可用性也未验证。

## 03 区：NOAA 极光与地磁数据

**目标：**把样本核对变成可运行、可验证的数据接入。

- [x] 在合理间隔内采集多个样本，保留抓取时间、原始时间字段、来源地址和内容校验信息，避免把一次成功访问当成稳定可用。
- [x] 核对 Kp 无偏移时间字符串的官方时间语义，以及 observed、estimated、predicted 三种记录的含义。
- [x] 分别建立 OVATION 和 Kp Provider；外部字段转为后端统一数据结构。
- [x] 校验网格结构、坐标和值范围、时间字段格式和空响应；统一处理 0–359 经度，明确接缝处理方式。
- [x] 分别返回获取时间、源观测/更新时间和预测目标时间。
- [x] 用 NOAA `Forecast Time` 作为数据有效性终点；超时后地图清除图层，地点等级回到数据不足。
- [ ] 根据足够的来源样本确定观测时间最大允许延迟；五分钟服务缓存不是 NOAA 刷新 SLA。
- [x] NOAA 全球数据共享缓存，不因每个用户查询重复下载完整网格；设置超时和失败状态。外部请求不自动重试，避免放大失败或额度消耗。
- [x] 测试正常网格、空网格、坐标越界、预报过期、未来观测时间、时间顺序异常和上游限流响应。
- [x] 补充空白、截断 JSON 和 `Data Format` 变化响应测试。
- [ ] 采集更多来源样本，评估最大允许观测延迟。

**验收：**两类数据分别展示状态和时间；过期数据不标为有效短时预测；Kp 不转成地点可见概率；OVATION 不被复制成未来两晚的预测。

**本区交付：**两个 Provider、可追溯样本和解析/失败测试。此时仍不计算观测等级。

**OVATION 进度记录（2026-09-24）：**已新增 OVATION Provider 与 `/api/v1/aurora-map`，验证了官方 JSON 的时间字段、`[Longitude, Latitude, Aurora]` 网格、0–359 经度换算和失败响应；12 项后端测试通过。官方数据采样脚本以 5 分钟间隔获取了 3 份约 924 KB 的完整响应，压缩快照和 SHA-256 manifest 保存在 `docs/data/noaa-ovation-samples/`。抓取时间为 11:50:00Z、11:55:03Z、12:00:10Z；三份响应均为 HTTP 200，包含 65,160 个坐标，ETag、Last-Modified、Observation Time 和 Forecast Time 均有变化，且压缩快照解压后的哈希与 manifest 一致。此次 10 分钟观测只证明这些样本期间数据持续更新，不构成长期可用性 SLA 或固定刷新周期。完整时效规则、缓存及剩余故障测试仍未完成。

**Kp 接入进度（2026-09-24）：**NOAA 官方三日地磁预报将 Kp 标为三小时 UTC 时段（如 `00-03UT`），实时 JSON 的 `time_tag` 与该时段起点一致，但不带 `Z` 或偏移；后端按 UTC 解析并在 API 中返回明确的 UTC `Instant`。响应中的 `observed`、`estimated`、`predicted` 被转换为枚举并原样保留语义，另保留 `kp`、`noaa_scale` 和抓取时间。新增只读 `/api/v1/kp-index`，不把 Kp 转为地点观测等级或概率。官方说明称 NOAA 图表展示三小时估算 Kp，并由多个地磁台站数据得出；单次实时样本用于验证 JSON 字段，不代表长期更新保证。验证：新增 provider 测试覆盖三类记录、UTC 解析、空/异常 JSON、越界 Kp 和 429；Kp 分类与缓存测试另见地图下方活动卡片记录。数据时效边界、长期稳定性和剩余故障样例仍未验证。时间解释参考 NOAA [三日地磁预报说明](https://www.spaceweather.gov/products/3-day-geomagnetic-forecast)、[官方预报文本](https://services.swpc.noaa.gov/text/3-day-geomag-forecast.txt) 与 [Planetary K-index 说明](https://www.spaceweather.gov/products/planetary-k-index)。

**地图下全球活动卡片（2026-09-24）：**新增 `/api/v1/kp-index` 前端展示，在地图下方显示下一条 NOAA `predicted` 三小时记录、Kp 值、活动等级、目标时段和数据获取时刻；日期时间按浏览器本地时区显示并包含偏移。活动等级用三档概括 NOAA 的 Kp 说明：低 `<3`（对应 0–2）、中 `3–<6`（对应 3–5）、高 `>=6`（对应 6–9）。依据为 NOAA 观测指南描述的 Kp 0–2、3–5、6–7、8–9 活动差异；三档合并是产品显示简化，不代表本地可见概率，也不替代完整的观测规则。后端共享 Kp 响应缓存 5 分钟。中英文界面均说明这是全球地磁/极光活动趋势，不是地点级可见性判断。后端测试新增阈值边界及缓存复用覆盖；前端 typecheck、lint、production build 通过。此卡片不改变三晚等级，仍为 `INSUFFICIENT_DATA`。

**NOAA 时间追溯进度（2026-09-24）：**OVATION 地图响应和地点活动响应现在分别包含 `observationTime`、`forecastTime` 与 `retrievedAt`。相同 NOAA 快照供地图和地点查询共享，`retrievedAt` 绑定缓存抓取时刻，5 分钟 TTL 仍只是服务端缓存策略，不被描述为来源刷新 SLA。地图以 UTC 显示三个时间，地点卡片按选定地点时区显示。固定时钟测试验证 API 时间字段及缓存复用。新鲜度过期门槛仍待更多来源样本支持。

**OVATION 过期数据进度（2026-09-24）：**用 NOAA `Forecast Time` 判定模型预报是否仍有效；当前时间晚于目标时间时，地图 API 返回 `EXPIRED` 且不带网格点，地点活动 API 返回 `EXPIRED`、`INSUFFICIENT_DATA` 并省略原始网格值。预测时间与当前时刻相等仍视为有效。Provider 拒绝晚于获取时间的 `Observation Time`，也拒绝早于观测时间的预测目标。测试覆盖过期、边界相等、未来观测时间和逆序时间字段；来源观测时间允许的最大延迟仍不设定，待采集更多样本后确定。

**NOAA 异常响应测试进度（2026-09-24）：**OVATION Provider 测试现覆盖空白响应、截断 JSON 和发生变化的 `Data Format`，三者均映射为 `INVALID_RESPONSE`。最大观测延迟仍需更多带抓取时刻的官方样本支持，当前不设固定阈值。

## 04 区：天气数据

**前置条件：**有真实可联系的项目网址或联系地址用于 MET Norway 请求身份；继续使用前复核官方使用条款。已使用项目 GitHub 地址和用户提供的邮箱配置请求身份。

- [x] 按条款配置服务端请求身份、署名、压缩和坐标精度。坐标向零截断到最多 4 位小数。
- [x] 分别验证北欧、爱尔兰和南半球地点的云量字段、单位、时间步长和覆盖范围。
- [x] 实现天气 Provider，保留原始时间粒度，不把稀疏预测伪装成逐小时实测。
- [x] 按 `Expires`、`Last-Modified` 和 `ETag` 缓存，并处理条件请求及 304；记录弃用响应，区分 403、429，其他 HTTP 错误不自动重试。
- [x] 将云量时间序列匹配到当地今晚（当地中午至次日中午）；来源缺失值和无覆盖保持明确。
- [x] 显示当地云量、获取时间、缓存有效期和署名。

**验收：**连续查询能复用缓存；服务失败、局部缺测和完全无覆盖分别可识别；不能把缺失云量填成 0%，也不能直接解释成观测机会低。

**本区交付：**天气事实接口、缓存行为证据和三类地点的样本验证记录。

**MET Norway 接入进度（2026-09-24）：**按官方条款以服务端 `User-Agent` 携带 Aurora Finder 项目链接和联系邮箱，请求坐标向零截断为最多 4 位小数，启用 HTTPS 重定向及 gzip/deflate 解压。真实样例在 Oslo（北欧）、Dublin（爱尔兰）和 Queenstown（南半球）均返回 HTTP 200；分别有 84、91、91 个预报时点，含 1 小时及 6 小时间隔，`cloud_area_fraction` 单位为百分比且本次全有值。完整响应快照及 HTTP 时间头、时间粒度、字段缺失统计和 SHA-256 清单保存在 `docs/data/metno-locationforecast-samples/`。实现 `/api/v1/weather-forecast`，按坐标缓存并支持 ETag/Last-Modified 条件请求、304 和来源过期时间；当地夜晚只筛选原始时间点，不插值，缺少云量留空。详情页天气卡片按当地时间呈现并链接 MET Norway、CC BY 4.0，标明筛选处理和未修改云量值。测试覆盖缓存复用、条件请求、gzip、403/429、非法云量、缺测、坐标精度和当地夜晚窗口；本机真实 Dublin API 验收返回今晚 20 个时点。三地样例验证不代表长期服务 SLA。

## 05 区：太阳高度与黑暗时段

**目标：**计算可解释的太阳位置，验证后再采用黑暗条件。

- [x] 比较候选 Java 库的许可、维护情况、时间语义、太阳高度定义及高纬度行为，选定具体版本。
- [x] 用独立参考计算核对普通纬度和高纬度、夏季与冬季的结果；记录方法、误差和适用范围。
- [x] 在 02 区夜晚区间内计算太阳高度，正确处理无日出、无日落和连续黑暗。
- [x] 为候选太阳高度阈值生成时间区间。候选阈值与最终观测规则分开记录；天文学定义不自动成为肉眼观测门槛。
- [x] 区分“没有满足条件的时段”和“计算失败”。

**验收：**极昼、极夜、跨午夜、夏令时和日期线案例有固定输入与可核对输出；不依赖一定存在的日出日落事件；阈值未验证时明确保留待定状态。

**本区交付：**天文计算服务、参考对照和边界案例。

**库选型记录（2026-09-25）：**选用 `net.e175.klaus:solarpositioning:2.0.13` 作为实现候选。项目采用 MIT 许可，要求 Java 17+、无运行时依赖；提供 NREL SPA 与 Grena3 太阳位置算法、太阳高度和暮光时段接口，文档说明超过 1,000 个参考测试点，并将结果区分为普通日、极昼和极夜。发布记录包括跨午夜的日出/日落处理修正。备选 `commons-suncalc` 为 Apache-2.0、Java 8+、无额外依赖，适合常见日出/日落场景，但项目自述精度约一分钟，并提示高纬地区日出/日落结果误差较大。NOAA Solar Calculator 页面目前明确说明已不再维护且不保证精度，因此只可作为带限制的参考页面，不能当作唯一验证标准。本选型只确定候选算法和版本，不代表计算结果已验证，也不决定肉眼观测的黑暗阈值。来源：[solarpositioning](https://github.com/KlausBrunner/solarpositioning)、[v2.0.13 发布记录](https://github.com/KlausBrunner/solarpositioning/releases)、[commons-suncalc](https://github.com/shred/commons-suncalc)、[NOAA Solar Calculator notice](https://www.gml.noaa.gov/grad/solcalc/index.html)。

**05 区完成记录（2026-09-25）：**后端 `SolarDarknessService` 采用 SPA 计算地点的几何太阳高度，在 02 区当地中午至次日当地中午的 UTC 区间内按 5 分钟步长扫描，并将阈值交叉点二分细化至 30 秒以内。每晚 API 返回民用暮光（太阳高度 ≤ -6°）、航海暮光（≤ -12°）和天文暮光（≤ -18°）各自的本地时间段；状态区分有时段、全窗口持续低于阈值、无时段和计算失败。前端中英双语显示这些时间段。阈值只描述太阳位置，不是最终观测建议或极光等级；没有应用地形遮挡、地平线高度或大气折射修正。独立对照使用 NREL SPA 报告中的 2003-10-17 Denver 基准输入，太阳天顶角和方位角测试与报告基准在 0.0003° 内一致；另测 Tromsø 冬季/夏季、高纬连续极夜/极昼、Dublin 春季 23 小时与秋季 25 小时窗口，以及日期线窗口边界。新增 5 项测试与全后端测试通过（38 项），前端 typecheck、lint、production build 通过。参考：[NREL SPA 技术报告](https://docs.nrel.gov/docs/fy08osti/34302.pdf)、[solarpositioning API 与边界语义](https://github.com/KlausBrunner/solarpositioning)。

## 06 区：事实汇总、三晚页面与地图

**目标：**先让用户看到有时间、有来源、可核对的事实。

### 06.1 统一事实服务

- [x] 建立 ObservationFactsService（观测事实服务），协调地点、夜晚、NOAA、天气和天文结果。
- [x] 每个来源独立携带状态、获取时间、源时间、适用范围、来源链接和失败原因；用枚举或受约束的状态类型。
- [x] 把短时、当晚趋势和未来夜晚的数据范围分开；检查时间覆盖是否重叠。
- [x] 局部失败时保留其他可靠事实，但不把部分成功写成完整数据充足。
- [x] 前端三晚卡片显示可用事实、缺失原因和源时间；响应生成时间不得替代提供商数据时间。

### 06.2 地图

- [x] 选择 MapLibre GL JS 作为地图渲染方式、MapTiler Cloud 作为底图服务；来源说明见 `docs/api-access.md`。
- [x] 保留可读的 MapTiler/OpenStreetMap 文字署名，并添加免费账户要求的 MapTiler 标志链接。
- [ ] 核对部署用途的底图许可与配额，并用 MapTiler Key 验收高纬度显示。
- [x] 地点详情页地图标记所选地点，并显示地点名和时区；可点击标记查看信息。
- [ ] 用浏览器验证南北半球、日期线接缝、缩放和窄屏交互。
- [x] 仅在状态有效、`forecastTime` 合法且未过期时渲染 OVATION 网格；地图标出预报有效时间，并解释这是模型活动信号。
- [x] 数据到达 `forecastTime` 时立即清空失效图层并重新请求；获取失败时清空旧数据。
- [x] 地图仅在首页显示；未来两晚详情页不复用当前短时图层。
- [x] 地图下方解释云量、黑暗、视野、地形和光害未包含在该图层内（中英文）。
- [x] 地图下展示 NOAA Kp 下一预报时段和有来源依据的全球活动分级，明确它不是当地可见性等级。

**验收：**真实地点能完成搜索 → 选择 → 三晚事实 → 地图及来源详情。浏览器检查加载、切换、错误和窄屏；故障注入确认各来源状态独立。此区可以持续显示“数据不足”，不要求提前产生高、中、低。

**本区交付：**共享事实契约和可交互的事实展示页面，是 Agent 后续复用的基础。

**06.1 事实服务完成记录（2026-09-25）：**新增 `GET /api/v1/facts/{locationId}`，统一返回当地三晚（含 SPA 黑暗时段）、NOAA 当地短时活动与 MET Norway 今晚云量。各来源用 `SourceFact` 返回枚举状态、各自时间范围、获取/源时间、来源 URL 和失败码。接口会明确标记 NOAA 短时范围与云量时点是否有实际重叠；不插值缺测时点，也不把来源可用性当成观测规则已验证。NOAA 或天气一方失败时，其他成功来源及三晚计算继续返回。详情页改为一次获取并每 5 分钟刷新聚合事实，逐来源显示状态、原始适用时间和缺失原因。测试覆盖单来源超时、独立状态/来源链接/时间范围及重叠检查；前后端构建和测试通过。06.1 完成，06.2 地图验收仍待进行。

**地图进度记录（2026-09-24）：**首页采用上方介绍、下方左侧大地图和右侧地点搜索/规则说明。Spring Boot 从 NOAA SWPC 读取最新 OVATION 网格，5 分钟内复用结果，页面标出来源观测时间和预测有效时间；MapLibre 将其渲染为可缩放的热度图。此前本机 API 检查返回全球非零网格点。2026-09-24 浏览器验收发现 MapLibre v6 在 Vite 中未配置 worker URL，导致 worker 加载失败且 `load` 事件不触发；配置 Vite worker 后，Chrome 已显示 MapTiler 底图、NOAA 极光热度层和时间戳，构建及 lint 通过，修复提交 `8a3ceab` 已推送。Key、样式及瓦片请求均返回成功。地图交互、窄屏、选中地点标记、过期数据移除和部署用途配额仍待验收，因此 06 区未完成。

## 07 区：观测等级规则

**本区有明确门槛：**数据接入成功不代表高、中、低的科学依据已经成立。没有足够依据时，完成规则研究记录，并保持“数据不足”；不能为了勾选任务随意制定百分比或阈值。

- [ ] 写明各等级针对肉眼观测的含义、适用地点/时段和局限。
- [ ] 为新鲜度、黑暗、极光空间关联、云量和数据冲突逐项记录依据、候选阈值和样本。
- [ ] 独立验证短时与未来夜晚的可判定范围。只有 Kp 趋势时，不自动承诺可生成某地点的远期等级。
- [ ] 写出带预期结果的决策表，确认缺失/过期/冲突先于等级计算处理。
- [ ] 把规则做成不直接发 HTTP 请求的独立计算服务，输入统一事实，输出等级、原因代码、证据和规则版本。
- [ ] 用边界值、反例和真实样本测试规则；区分“满足代码预期”与“预测能力已经得到验证”。

**验收：**相同事实和版本得到相同结果；每个等级可追溯到输入及规则；无法支持的日期或地区仍返回数据不足；输出没有伪造的看到极光百分比。

**本区交付：**规则说明、版本化实现、决策表测试，以及尚未验证的适用范围清单。

## 08 区：运行记录与持久化

**目标：**使一次结果能被追查，为后续工具轨迹提供存储基础。此区再引入 Repository 和 Entity。

- [ ] 明确需要保存的最小记录、用途、保留期限和清理方式，再选择数据库。
- [ ] 设计判定运行记录：运行 ID、规则版本、执行时间、适用夜晚、结果和必要的来源证据引用。
- [ ] 若要求重现旧结果，保存足够的输入快照或可恢复的内容引用；只存来源 URL 或哈希不能单独保证重现。
- [ ] 评估坐标、用户输入和工具参数中的敏感信息，控制存储精度、访问和保留范围。
- [ ] 建立 Entity、Repository 和数据库迁移；事务由 Service 协调。
- [ ] 规则可以先随代码版本管理；短期缓存可以先在内存中实现，不强制为两者各建表。
- [ ] 为 09 区预留运行与工具调用的关联方式，具体字段随工具契约确定。

**调用关系：**Controller → Service；Service 按需调用 Provider 获取外部数据，或调用 Repository 读写数据库。DTO 是接口数据形状，Entity 是持久化映射，两者不等同。

**验收：**记录能在应用重启后查询；能解释一次结果对应的规则和来源；迁移可在空数据库执行，保留与清理策略有验证；数据库故障不会被误报为观测条件低。

**本区交付：**实际有用途的数据表、迁移脚本、持久化实现与必要的集成测试。

## 09 区：Agent 问答

**前置决策：**模型/调用方式、单次与总体成本上限、工具超时、保存哪些轨迹、是否保存聊天正文及保留期限。此区前半可先写工具契约；正式模型调用和保存用户内容必须遵循已确定配置。

- [ ] 为地点解析、夜晚事实、判定结果、来源时间提供只读工具，共用已有 Service。
- [ ] 工具参数包含明确地点和日期；同名地点、模糊日期、缺失地点时追问。
- [ ] 让 Agent 解释后端结果及局限，回答引用工具事实和时间，不自行计算等级或百分比。
- [ ] 添加工具次数、输入长度、超时及费用限制；模型和提供商失败均有可理解的提示。
- [ ] 保存可回放检查的工具调用轨迹：调用顺序、参数、必要的返回事实、时间、错误及最终结果关联，并按策略脱敏。
- [ ] 接入可用的问答界面，清楚表达等待、追问、失败和数据不足。
- [ ] 问答采用可展开/可收起的窄聊天面板；默认收起，点击入口展开，在面板角落点击可收起。首次打开且对话为空时显示可点击的示例问题，帮助用户知道能询问哪些地点、时段、活动等级和天气事实；不依赖已保存的历史对话。输入栏始终清楚可见。
- [ ] 评估同名地点、跨时区日期、极昼、云量缺失、NOAA 过期、百分比请求和工具失败。

**界面要求记录（2026-09-25）：**用户希望 AI 入口打开为一个较窄的 chatbot 窗口，可从页面角落按钮展开，并能从窗口角落收起；空白对话时展示示例问题/提问灵感，说明 Agent 可以提供哪些信息。前端中英文文案都要支持。聊天记录是否跨会话保存仍未决定，因此空白态提示不能依赖持久化历史。

**验收：**同一地点、时段和事实下，页面与 Agent 的等级及证据一致；Agent 不能越过规则服务生成新结论；轨迹能解释一次回答为何产生。

**本区交付：**受约束的只读 Agent、成本配置、工具轨迹和评估报告。

## 10 区：部署、端到端验收与演示

- [ ] 部署 Java 后端；在 Vercel 设置 `VITE_API_BASE_URL` 为后端地址，在后端设置 `APP_CORS_ALLOWED_ORIGINS` 为前端正式域名，验证生产 API 路由。
- [ ] 在 MapTiler Key 中允许 Vercel 正式域名，并确认地图样式及瓦片请求成功。
- [ ] 配置服务端密钥、请求身份、数据库连接、超时和健康检查；确认启动与迁移步骤。
- [ ] 从部署后端重新检查各提供商访问、缓存、限流及来源时间，记录环境差异。
- [ ] 运行完整流程：搜索 → 选择 → 三晚事实/等级 → 地图 → Agent → 工具与判定记录。
- [ ] 覆盖北半球高纬度、爱尔兰、南半球、极昼/极夜和日期线；线上实际查询与固定历史测试分别记录。
- [ ] 检查提供商超时、过期、缺测、模型失败和数据库故障的降级体验；检查请求量、响应时间和外部调用成本。
- [ ] 配置构建、必要测试和部署检查；准备失败后的恢复方式。
- [ ] 更新英文 README、运行/部署说明和演示材料，明确实际完成内容、验证范围及未覆盖限制。

**线上故障检查记录（2026-09-24）：**刷新 `https://aurora-finder.vercel.app/` 后，浏览器仍能渲染页面框架；空白地图和不可用数据来自底层请求失败。`/api/v1/aurora-map` 与 `/api/v1/kp-index` 返回 404，说明 Vercel 部署没有这些 Spring API 路由；MapTiler 样式请求返回 403。前端现支持 `VITE_API_BASE_URL` 并在生产环境未配置时显示提示，后端现支持 `APP_CORS_ALLOWED_ORIGINS`。这些代码不能代替部署后端、设置 Vercel 环境变量或调整 MapTiler Key；完成这些外部配置后仍需重新验收。

**验收：**逐项满足 PROJECT_PLAN.md 第 7 节；真实线上流程与故障场景均有证据。前端构建成功、服务启动成功或单次 HTTP 200，都不能单独算 MVP 完成。

**本区交付：**可运行的线上版本、验收记录、演示步骤和准确的项目说明。

## 每一区统一的工作节奏

1. 阅读当前代码、该区任务和前一区交付，确认已完成部分，避免重做。
2. 选择一个可验收的小任务，列出修改位置和输入输出；缺少必要信息时记录阻塞项，同时推进不依赖它的工作。
3. 完成实现及针对实际风险的检查。解析和规则测试使用固定样本；真实提供商访问作为单独集成验证，避免普通测试依赖外网。
4. 前后端连通后检查实际页面与 API；修复问题后再勾选任务。
5. 更新进度、决策和验证证据，提交清晰的代码变更，再进入下一任务。

每一区结束时记录：完成项、涉及文件、验证命令与结果、尚未通过的门槛、下一项工作。测试未通过、规则未验证、依赖信息缺失，都不写成“该区已完成”。

## 可以直接用于后续开发的指令

> 请按 DEVELOPMENT_GUIDE.md 开发第 01 区。先核对当前实现，一项一项完成本区任务，针对实际问题做验证，并更新本区进度和验收记录。遇到需要我提供的信息请明确指出，同时继续不依赖它的工作。保持观测等级未验证时返回数据不足。

后续只需替换区号。若只想推进一个小任务，可以补充“本次只做 02 区的今晚定义和时间边界测试”。

## MVP 之后的功能区

保持原顺序：A 单地点全球查询（本指南 01–10 区）→ C 自动推荐附近候选观测点 → B 用户指定 2–3 个地点比较。

- C 区必须先验证观测点来源、路程时间、开放与可达性和安全信息，再支持“最多开车一小时，去哪里更值得试”。比较能力可以作为内部实现，但数据未满足时不能推荐具体地点。
- B 区在 C 区之后开放手动地点比较，复用地点、时间、事实和规则服务。

完成 MVP 验收时，应主动提醒进入 C 区，而不是自动添加登录、支付、主动推送或其他未规划功能。
