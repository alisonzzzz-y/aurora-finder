# 极光观测机会助手：项目规划

状态：地点搜索、三晚当地日期、首页 NOAA OVATION 地图、NOAA Kp 全球活动卡片，以及地点页的 NOAA OVATION 短时当地活动等级已完成本机验证。接口分开返回观测时间、预报目标时间和获取时间；预报超过有效时间后地图清除旧网格、当地活动等级返回数据不足。当地活动等级描述最近网格点的模型信号，不是个人观测概率；云量、黑暗时段、观测时间最大延迟规则和整晚观测等级仍待完成。最后更新：2026-09-24。

分区执行顺序、任务清单和验收标准见 [DEVELOPMENT_GUIDE.md](DEVELOPMENT_GUIDE.md)。

## 项目语言约定

本项目面向英文招聘与作品集展示。代码中的类名、变量名、API 字段、注释、测试名称、提交信息，以及应用界面文案均使用自然、清楚的英文。README、部署说明、架构说明和其他对外展示的文档只写英文，不附中文对照。内部规划文档（包括本文）可以使用中文。英文描述必须忠于实际实现与数据权限，不能把模型预测写成已观测事实，也不能夸大个人贡献。

## 1. 产品目标

面向偶尔想看极光的普通人。用户输入全球任意地点，查看今晚及未来两晚的观测条件，理解哪些因素有利、哪些因素不利，以及数据有多新。页面提供明显的 AI 问答入口，帮助用户用自然语言询问某个地点和日期的观测机会。

本产品提供有来源的观测建议，不承诺用户一定能看到极光。默认讨论肉眼观测；若以后支持摄影模式，必须单独定义规则和文案。MVP 不把综合结果称为“看到极光的百分比概率”。

## 2. 已确定的 MVP 范围

1. 用户搜索并选择一个全球地点，系统解析为经纬度和时区；必要时允许用户确认同名地点。
2. 展示该地点当地今天及未来两晚。默认日期按地点当前的当地日历日期确定，未来日期按当地日历递进；当地凌晨查询时，“今晚”仍指当天稍后到来的夜间。每个日期的候选事实匹配窗口为当地中午至次日当地中午，以便把傍晚与跨午夜后的时段归为同一晚；这不是实际黑暗或可观测区间，黑暗时段须另行计算。页面中的日期和时刻都按地点时区显示；日期旁标当地日开始时对应的 `UTC±HH:MM`，数据时间旁标该时刻实际对应的偏移。服务内部可用 UTC instant 交换和比较时间。
3. 提供交互地图，标出用户地点，并在有有效 NOAA OVATION 短时预测时显示南北半球的极光活动范围或强度分布及预测时间。地图展示模型预测的极光区域，不将边缘画成“区域内一定看得到、区域外一定看不到”的地面可见边界。地图还需说明云量、黑暗、地形视野和光害不包含在这层极光数据内。未来两晚没有同等空间预测时，不把当前极光范围沿用到未来夜晚。
4. 展示客观数据：极光活动或地磁预测、对应时间与预测范围、当地云量、是否处于足够黑暗的时段、每个来源的更新时间。数据缺失或过期时明确标记。
5. 短时判断使用 NOAA OVATION 极光位置与强度预测；它主要覆盖约 30–90 分钟。较远日期使用适用的 NOAA Kp 预测作趋势参考，不把 Kp 当成某地点的直接可见概率。
6. 页面有醒目的 AI 入口，可采用右下角浮动按钮。Agent 能理解地点、日期、时区和用户追问，调用只读工具获取结构化事实，再解释后端给出的等级与依据。页面查询和 Agent 共用同一套事实与判定服务。
   - AI 入口展开为窄聊天面板，入口/面板角落控件可以展开和收起；面板有清楚的消息区与输入栏。
   - 新会话的空白状态显示可点击的示例问题，给用户提问灵感并介绍可查询的信息。该体验不依赖保存聊天历史；历史记录是否跨会话保存另行评估。
   - 聊天面板、空白状态提示、等待/失败/数据不足文案均须支持中英文。
7. 页面至少提供来源链接、数据时间、预测与观测的区别、结果的局限说明。MVP 不要求登录、支付或主动推送。

## 3. 数据来源与可行性门槛

| 所需信息 | 候选来源 | 已知边界 | 开工前验证 |
| --- | --- | --- | --- |
| 短时极光活动与地图 | NOAA OVATION JSON：https://services.swpc.noaa.gov/json/ovation_aurora_latest.json | 全球南北半球网格；预测时段短；网格强度不是某个用户的肉眼可见概率 | 验证字段、经纬度格式、观测与预测时间、更新节奏、异常或空数据；核对地图投影与经度接缝处的绘制 |
| 当前及未来 Kp | NOAA：https://services.swpc.noaa.gov/products/noaa-planetary-k-index-forecast.json | Kp 是全球地磁活动指标；较远期判断要降低确定性 | 验证观测、估计、预测记录的标记和日期范围 |
| 地点云量 | 挪威气象局：https://api.met.no/weatherapi/locationforecast/2.0/compact | 全球覆盖；不同地区的模型精度和更新频率不同；必须按条款标识请求、缓存并署名 | 在北欧、爱尔兰、南半球各测至少一个地点，检查云量字段与时间 |
| 地点搜索与时区 | 待选定来源或本地库 | 不能只凭城市名称推测唯一位置；跨日期线与夏令时要正确 | 核对覆盖、免费使用限制、署名和请求配额 |
| 天黑时段 | 待选定可信的日出日落/太阳高度计算方式 | 高纬度可能整夜不够黑或出现极昼 | 验证夏冬、高纬度、跨午夜时段 |

此前已手动看到核心接口返回数据，但“浏览器能打开”只证明基础访问。Phase 0 还需从本机和计划部署的后端分别调用，检查字段、时间、连续更新、使用条款与失败行为。任何提供商失败都不能被解释为“观测机会低”。

## 4. 观测等级与事实边界

- 由后端的版本化规则计算等级，AI 不自行计算或编造等级、百分比、云量、极光强度和来源。
- 优先判断数据是否足够新、该时段是否足够黑暗、地点附近是否有可用的极光预测，再结合云量等因素。确切阈值必须在读取真实样本、核对官方解释后确定，不在规划阶段随意写死。
- 短时预测、当晚趋势、未来两晚趋势分别展示可信度或范围，不把不同预测时效混成一个“实时概率”。
- 若关键数据缺失、过期、互相冲突或不适用于该地点，显示“数据不足”并指出原因。
- 结果表达为“当前条件是否值得尝试观测”。不能声称已经检测到用户头顶有极光，也不能保证肉眼可见。月光、地形、北向或南向视野、当地光害等未被可靠纳入时，需要明确说明。

## 5. Agent 在 MVP 中的实际任务

示例问题：“周五晚上在都柏林值得出去看吗？”

Agent 的可用只读工具计划：地点解析；获取该地点的观测事实；查询指定本地夜晚的云量和黑暗时段；获取后端计算的等级与证据；读取来源和更新时间。模型根据问题选择所需工具，发现缺失地点或模糊日期时追问，发现来源过期时说明无法确认。工具返回结构化数据；最终回答引用事实和时间，不生成新事实。

第一版不允许 Agent 任意浏览网页、操作用户账号或给出未经验证的观测地点。需要保存完整的工具调用轨迹，便于调试和面试展示。评估案例至少覆盖：同名地点、跨时区日期、极昼、云量缺失、NOAA 数据过期、用户要求百分比概率。

## 6. 技术结构

- 前端：React + TypeScript，采用简单类型。主要页面为地点搜索、三晚结果卡片、可交互的短时极光活动地图、客观数据与来源详情、显眼的 Agent 问答入口。前端不直接拼接 NOAA 和天气数据得出结论。
- 后端：Java Spring Boot，保持单体应用，按职责分包。Controller 处理 HTTP 输入输出；Service 负责地点/时间协调、观测规则、Agent 工具服务和事务；需要持久化时加入 Repository 和 Entity，分别负责本项目数据的读写与持久化映射。DTO 用于数据传输，不作为独立调用层，也不替代 Entity。
- 外部 NOAA、天气、地点搜索 client 放独立的 `provider` 包；配置和 DTO 放辅助包。不把外部 HTTP 请求伪装成 Repository。
- MVP 数据库只存真正需要持久化的内容，例如规则版本、运行记录或缓存元数据。是否保存聊天历史、收藏地点，待隐私与产品需要明确后决定。不为凑四层而创建空表。
- 后端对提供商调用设置超时、缓存、有限重试和独立失败状态。API key 若未来出现，只存服务端配置，不提交到仓库或暴露给前端。

## 7. MVP 完成标准

1. 任意支持的全球地点能完成“搜索 → 选择 → 三晚结果 → 地图及证据”的完整流程；同名地点不误选。
2. 有有效短时数据时，地图能正确显示南北半球的极光活动分布、用户地点和预测时间；数据过期时停止显示旧范围，并且不将活动范围误称为地面可见范围。
3. 对短时 NOAA 预测、较远期 Kp 趋势和云量显示正确时间与来源；不同数据时效有明确文案。
4. 关键源过期、缺失或失败时，页面和 Agent 都给出一致的“数据不足”或降级结果，不捏造事实。
5. 至少有若干真实地点的端到端验证：北半球高纬度、爱尔兰、南半球，以及极昼或跨日期线案例。
6. 观测等级规则与 Agent 工具行为有自动化测试；部署后用真实 API 做一次线上流程验证，并记录使用限制与结果。
7. README 准确区分已实现功能、待实现功能、数据来源及限制。可在简历和演示中解释个人完成的工程工作。

## 8. MVP 后路线与提醒

### 8.0 三天地磁风暴展望（已接入首版）

首页从 NOAA SWPC 三天地磁预报文本读取每日活动/风暴等级概率，并与现有三小时 Kp 预报按 UTC 日期并列展示；另从 NOAA alerts.json 提取仍在有效期内的 K-index 4/5 预警及其 UTC 生效窗口。概率指地磁活动达到相应类别的预报，不是某地点看到极光的概率；Kp 峰值时段也不代表精确爆发时间。概率接口保留发布时间和获取时间，采用 30 分钟缓存，并验证返回类别、日期范围及百分比；预警接口采用 2 分钟缓存，只展示尚未过期的有效窗口。

“较佳观测地点”目前显示为待数据支持状态。只有候选地点名单有明确依据，且同一预报时间窗具备当地极光活动、云量和黑暗时段数据后，才开发地点排序。不得将全球 Kp 概率映射为城市可见概率。

MVP 完成后，按下列方向扩展。热门地点展示、观测条件时间轴和增强的数据来源详情优先评估；提醒、自动候选点推荐和手动多地点比较随后推进。每项扩展继续遵守来源、覆盖范围、更新时间和不确定性要求，不输出未经验证的个人观测概率。

### 8.1 热门观测点实时数据栏

在首页或全球活动地图下方增加一个长期保留的热门地点区域，交互上类似世界时钟：同时列出若干热门观测点（候选城市及选择规则待确定），用户不必逐个搜索就能快速浏览。它是页面内容区，不遮挡地图，也不固定悬浮在视口上。

- 每个地点展示当地时间、附近 NOAA OVATION 模型活动等级/数值、数据更新时间与预报有效时间；如接入了有覆盖的数据，可补充云量和当地黑暗时段。
- “热门”名单需有明确依据并允许后续调整，不能把城市列入名单暗示它当下适合观测。
- 只对数据覆盖的时段展示相应事实。源数据过期、缺失或来源不可用时显示状态，不沿用旧值，也不把活动等级说成看到极光的概率。
- 先验证热门点列表、请求数量、缓存和更新频率，避免每个地点各自频繁请求外部服务。

### 8.2 观测条件时间轴

评估并实现按所选地点当地时间排列的时间轴，帮助用户同时阅读极光活动、云量和太阳黑暗时段。技术上可行，但各来源的预测时间范围并不相同，第一版不得把 NOAA 的短时 OVATION 预测延伸成整晚预测。

- 每个数据系列保留自己的来源、时间戳和有效范围；只在时间确实重合的位置并列显示。
- OVATION 只覆盖其实际短时有效窗口；云量按天气源返回的小时点展示；黑暗时段使用已验证的太阳高度计算结果。
- 时间不重合或数据点缺失时明确留空或标记“无数据”，不得插值成看似精确的综合观测分数。
- 验收应覆盖跨午夜当地日期、夏令时、极昼/极夜、预报缺口和来源过期情形。若共用时间轴反而掩盖了各自覆盖范围，应改为分行并列显示。

### 8.3 数据质量与来源详情

MVP 已要求展示主要来源状态、时间范围和数据更新时间。后续可在此基础上提供可展开的来源详情，解释数据质量，而不是另造一个汇总质量分数。

- 每项显示提供商、指标含义、获取时间、观测/预报时间、有效范围、缓存状态及失败/过期原因。
- 标明数据缺失、延迟、覆盖不完整或来源之间时间不匹配；保留原始来源链接与署名。
- 质量状态采用可测试的来源级规则；阈值须依据提供商更新节奏和样本验证后设置。

### 8.4 强活动事件提醒

当前首页的三天地磁展望是 NOAA 概率预报展示，不是用户订阅的事件提醒，也不会预测精确爆发时刻。后续提醒仍需单独定义事件阈值、时效、去重、结束条件、误报处理和通知渠道。

当实时/短时模型活动满足经过验证的条件时，提供用户可订阅的强活动提示。提醒代表模型信号达到规则条件，不宣称已经观测到爆发，也不保证用户所在地可见。

- 开发前定义事件阈值、时效、去重、结束条件、误报处理和通知渠道，并用历史或持续采集样本评估。
- 明确事件覆盖的半球、区域、预测有效时间和所依据的数据源；来源过期时停止触发。
- 通知方式、频率限制、用户偏好与隐私要求一并设计；如需保存订阅或地点，先定义数据保留策略。

### 8.5 附近地点推荐与比较

优先顺序已确定：**A 单地点全球查询 → C 系统推荐附近候选观测点 → B 用户指定 2–3 个地点比较**。C 的内部会用到地点比较能力，但对外先交付自动推荐，再开放手动比较。

**必须在 MVP 完成后提醒：**支持用户提出“我在都柏林，今晚最多愿意开车一小时，去哪边更值得试？”之类的问题。系统比较候选点的云量和极光条件。实施前要取得并验证可信的观测点、路程时间、开放/可达性与安全数据；这些条件未满足时，不推荐具体地点。此功能不属于 MVP。

### 8.6 可选后续想法

- 收藏常看地点或建立个人地点清单，方便与热门地点区域并用；需要先确定是否登录及地点偏好保存方式。
- 保存强活动事件的历史记录，帮助用户回看模型发布时间和数据变化；仅在具备足够连续、可追溯的历史数据后评估，不能把模型记录描述为地面观测记录。
- 提供分享链接或简洁的观测计划摘要，内容只包含可追溯的数据、时间和来源。

## 9. 下一轮待定问题

1. 地点搜索采用 Open-Meteo Geocoding API 并返回 IANA 时区；天黑计算仍需选定并验证可信来源或库，核对条款和全球边界。
2. “高 / 中 / 低”对普通人的默认含义如何落成可测试规则？是否保持默认肉眼观测？
3. Agent 的模型与调用方式、成本上限、聊天记录是否保存？
4. 首版应用界面只使用英文；是否在后续增加其他语言？

## 参考来源

- NOAA 极光模型与数据：https://swpc-drupal.woc.noaa.gov/products/aurora-30-minute-forecast
- NOAA 极光观测建议：https://www.spaceweather.gov/content/tips-viewing-aurora
- 挪威气象局全球天气模型：https://docs.api.met.no/doc/locationforecast/datamodel.html
- 挪威气象局 API 使用条款：https://docs.api.met.no/doc/TermsOfService
